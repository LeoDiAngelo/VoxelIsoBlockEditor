<Application x:Class="IsoBlockCharacterEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="Background" Value="#263247"/>
            <Setter Property="Foreground" Value="#F3F7FF"/>
            <Setter Property="BorderBrush" Value="#40506A"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="Background" Value="#101722"/>
            <Setter Property="Foreground" Value="#F3F7FF"/>
            <Setter Property="BorderBrush" Value="#40506A"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Background" Value="#101722"/>
            <Setter Property="Foreground" Value="#F3F7FF"/>
            <Setter Property="BorderBrush" Value="#40506A"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#C3CDDD"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#C3CDDD"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </Application.Resources>
</Application>
